# Interop corpus provenance

Copied verbatim from `bluesky-social/atproto`, path
`interop-test-files/syntax/nsid_syntax_{valid,invalid}.txt`, on 2026-08-14.

Do not hand-edit. To refresh, re-download from upstream and run the test suite;
a diff in behaviour is the signal this crate needs updating, not the corpus.

The same two files are vendored in `packages/nsid/tests/interop/`. They must stay
byte-identical — `packages/nsid` has a test that asserts it.
